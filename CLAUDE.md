# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Coding Standards [WRITTEN BY HUMAN - DO NOT OVERWRITE]
- HH型マルチコンパートメントニューロンの一部ノードをSINDyで抽出したサロゲート方程式に置換し、演算コスト削減と波形再現性を評価する研究コード
- 一時変数は同じ値を何度も使うような場合にのみ許可
  - NG: `x = obj.attr; f(x)`
  - OK: `f(obj.attr)`
- 大きな改装が終わったら、just test でエラーが出ないことを確認,tests/ 以下のテストは自由に追加して良い ただし、20s以下に抑えること
- Hooksで実行されるjust lint、just formatのエラーは都度対処すること
- 研究のまとめは、docs/poster、docs/slideディレクトリ以下にtypstとしてまとめる

[以下のセクションは永続メモリとして上書きしても構いません。
ただし、30行以内程度に納め、本当に必要な情報だけを圧縮して書くこと]

## Commands

```bash
uv sync                                                           # 初期セットアップ（依存導入）
uv run scripts/main.py                                            # 実行 (fit+MLflow log のみ。kernel は回さない)
uv run scripts/main.py surrogate=_hh_informed                     # Hydraプリセット切替 (base=hh_full/traub_full、lib違いは _hh_informed/_hh_relaxation)
uv run scripts/main.py --multirun                                 # config.yaml の hydra.sweeper.params 直積 sweep (hh/traub × type{sindy,hybrid} × n_components{1,2} × preprocessor{pca,ae} = 16 run)
just test                  # pytest (tests/、Hydraプリセット読込→fit→置換シミュ→指標/描画) + main.py + marimo export
just format && just lint   # ruff fix+format / ruff+mypy (strict、scripts/ 除外)
just mlflow                # MLflow UI (port 5100、backend: mlflow.db)
just marimo                # marimo notebook (port 2700)
just marimo-mcp            # Claude Code MCP連携 (port 2701)
just clean-cache / clean-log
```

## Architecture

**Data flow**: Hydra conf → `DatasetConfig`/`NeuronGraph` → `NeuroSurrogateBase.__init__` (`unified_simulator()` JAX Euler+`lax.scan` → `_train_gate()` で preprocessor build) → `.fit()` (PySINDy → `FeatureLibrary` から `compute_theta` 直接構築) → MLflow log。`neurosurrogate/__init__.py` で `jax_enable_x64` 強制 ON (HH ゲート exp 数値安定化に必須)。

**neurosurrogate/core/**: `network.py`(`Compartment`/`CompartmentType`/`NeuronGraph`/`DatasetConfig`/`CurrentConfig`。`chain(...)` ノード自動命名。置換の type 差替は surrogate/replace.py に移管、core は純粋データ構造) / `simulator.py`(`unified_simulator` + `calc_universal_deriv` が type 名 dispatch で `vmap` 一括計算、`generic_euler_solver` が `lax.scan`) / `coords.py`(xarray 座標の **write** 側。features=MultiIndex(comp_id,variable,gate)/gate=False→電位/node_id→I_internal を組立。`set_latent_coords`(V+latent 組立) + `transform_gate`(preprocessor で gate→latent 射影、ansatz/metrics 共用)) / `access.py`(同スキーマの **read** 規約を集約する唯一の場所。生 `sel` は全域禁止しここ経由。`potential`/`gate_matrix`/`comp_matrix`/`potential_matrix`/`i_ext_values`/`i_internal_values`/`time` 等 numpy accessor(計算層) + `trace`/`i_ext`/`i_internal` 等 (t,y) accessor(描画層)) / `opcost.py`(`OpCost` 代数、`+`/`*` で演算コスト集計)

**neurosurrogate/{compartments,models,currents}**: `compartments/{hh,traub,common}.py` 動力学 + `COMPARTMENT_TEMPLATES` (`hh.py` に `HH_RATE_ENTRIES`/`HH_GATE_PAIRS`/`HH_GATE_FORWARD`) / `models/` に `MCMODELS` + `traub19.py`(19-comp、traub.c 代数的等価) / `currents.py` に電流波形 + `CURRENT_MAP`

**neurosurrogate/surrogate/**: `bundle.py`(**サロゲートの主体**。`SurrogateMeta`(surrogate_type/dataset/train_comp_id/n_components/preprocessor_kind + `simulate`/`label`/`train_comp`) と `SurrogateBundle`(meta/preprocessor/sindy_bundle/train_xr を保持し ansatz を差替えて駆動するオーケストレーター。`setup(type,datasets,train_comp_identifier,n_components,preprocessor)` classmethod が meta 構築 → simulate → `ansatz.train_gate()` で preprocessor build まで、`fit` は SINDy 同定のみ (責務二層分離)。`save`/`load`=`surrogate.joblib` 単一ファイル pickle。`surr_comp_type`/`opcost`/`params_compatible`/`metrics` は ansatz へ委譲。`ansatz` は meta.surrogate_type から解決する cached_property=状態なし→保存不要。**setup と load が別経路** → load は 3 点を戻すだけで simulate が走らない。preprocessor は setup 時・sindy_bundle は fit 時・train_xr は setup 時に埋まる (未設定参照は AttributeError))。外部が触る型は一貫して `SurrogateBundle` (main.py/mlflow_io/analysis/metrics/replace すべて) / `ansatz/base.py`(`Ansatz`=方程式の定式化 (列構造・kernel・演算コスト) のみ担う抽象基底。**状態を持たず bundle を第1引数で受けるストラテジ**: `train_gate(bundle)`/`fit(bundle,optimizer,specs)->SINDyBundle`/`surr_comp_type(bundle)`/`opcost(bundle)` の4本のみ) / `replace.py`(置換ドメイン一元化。**互換性判定もここに集約** (ansatz でなく置換の関心事): `_PARAMS_MATCH` が surrogate_type → params 両立基準 (sindy=surr が param_cls=None で学習モデルが params 込み再現→全一致必須 / hybrid=物理 dv も Ca も置換先自身の params で解く→型一致のみ)、`replaceable(bundle,comp)` 型一致+params 両立、`replaceables(bundle,ds)` fail first (型一致だが params 非両立が有る or 対象皆無で `ValueError`)、`replaced_names` 非raise 診断用、`apply_surrogate(bundle,ds)` が対象ノードを `replace_nodes`+`dc_replace` で非破壊 type 差替) / `sindy.py`(`SINDyNeuroSurrogate`、列構造 [V,latent…,u]) / `hybrid.py`(`HybridSINDyNeuroSurrogate`、列構造 [latent…,V] (u無)、AE preprocessor 利用可、n_components 3 が既定。学習データ源は `_train_comp_ids`=`replace.replaceable` 真のノード全部 (**置換する comp の軌道で学習する**→訓練分布=評価分布。単体モデルなら 1 個で従来同一、`model_name: traub19` 等 multi-comp 指定で全 comp の (V,gate) 軌道が入る。学習ゲートは params-free→全 comp 同一多様体、増えるのは V 被覆のみで n_components 据置き可)。`_train_gate`=先頭 n_learned ゲート (Ca サブ系は physics 分離) を**縦連結** (preprocessor は時間微分を取らない)、`fit` は**軌道リストのまま** pysindy へ (縦連結すると境界に偽微分))。各 ansatz は `_train_gate` (列選択) と fit 内 `Roles` を n_components から inline 構築 (sindy=全ゲート列) / `roles.py`(`Roles`: V=電位列/g=ゲート列群(=隠れ変数)/u=外電流列。方程式構造=ansatz ドメイン。`bind` 1本で項の args シンボル名 V/g/u を列へ束縛、sindy/ の項は duck typed 消費) / `sindy/`(SINDy 同定層。`__init__.py`=`SINDyBundle` (xi/feature 展開/compute_theta/opcost。`from_sindy` の x/u/t は list を渡すと複数軌道) + `OPTIMIZER_CLS`={"stlsq"}、項ライブラリはロジック/カタログ分離。項=1つの sympy 式が真実源→func(lambdify)/name(subs)/cost(op_cost)/args(free_symbols を V,g,u 順)/arity 全派生。HH レート関数は数値安定 exp 保持のため未定義 Function シンボル+lambdify 注入。`entry.py`=ロジック(`op_cost` 木辿り→OpCost/`LibraryEntry`(expr,args,func。`argnames`=列束縛キー兼 展開規則の判定源)/`_group_by_argnames` 同一 argnames で 1 束/`SubLibrary.expand(spec,roles)` は spec={type,latents?} のみ受け (番号=latent序数、既定全)、展開は args が決定: g 持ち群=選択 latent へ複製・g 無し群=1束・u 持ち群=u 無し ansatz では脱落/`FeatureLibrary.build(specs,roles)` は catalog を遅延 import) / `catalog.py`=データ(`_entry`(args 自動導出)+`_R` レート式+項カタログ `LIB_ENTRIES` 1本。**全 type は互いに素** (同じ式を2 type が持たない。合成は yaml 側の責務: 例 forward+backward=全レート、forward+relaxation_decay=緩和形)。`_rate_types(prefix,pairs)` が (alpha,beta) ペア列から {gate_forward,gate_backward,gate_forward_product,gate_backward_product,relaxation_decay} を派生 → `hh_*` (3ゲート) / `traub_*` (V依存8ゲート、Q は XI 依存で対象外) + gate_poly_volt (latent 高次・V積。1次は basis) + basis。レート実装注入は `_RATE_NS`=vars(hh)|vars(traub)、コストは entry.py の `_RATE_COST_MAP`=HH|TRAUB。basis=V/u/latent 射影+定数1(V に相乗り) で他項と同じ規則に還元、latents:[] で V/u/1 のみ)) / `preprocessor/`(gate↔latent 可逆変換、ansatz/ と同型パッケージ。`base.py`=`Preprocessor` ABC (encode/decode/metrics/opcost、`_set_fit_artifacts` で再構成統計+初期潜在。dataclass でなく明示 `__init__`) / `pca.py`=`PCAPreprocessor` / `autoencoder.py`=`AEPreprocessor`+JAX-AE 学習 / `__init__.py`=`PREPROCESSOR_CLS`{"pca","ae"} レジストリのみ (組み立ては `bundle.setup` が `PREPROCESSOR_CLS[type].fit` を直接呼ぶ)) / `__init__.py` に `SURR_CLS` 辞書 (`sindy`/`hybrid`)

**neurosurrogate/{metrics,view}**: `metrics/wave.py`(eFEL スパイク + RMSE/MAE、V 抽出は `core.access` 経由) / `view/{engine,specs,model}.py`(`draw_engine`/`TraceSpec`(t,y numpy 保持で Dataset 非依存)/`PanelSpec`/`error_fig` は engine、`plot_2d_attractor_comparison`+`draw_all(original,surrogate,comp_id,get_preprocessed)` (内部 jobs dict で全描画一括、失敗を error_fig に畳み識別子付き fig 列返却) は specs、`view_neuron_graph`/`view_model`+`model_figures` (識別子付き model 図列) は model、`current_preview_fig`/`sweep_fig` (marimo 非依存の純粋描画。analysis は UI 値引き出しのみ担い委譲) は utils。**view が (id,fig) 列を提供し analysis は保存/表示に流すだけ**。Dataset 掘削は `core.access` 経由)

**scripts/**: `main.py`(Hydra エントリ) / `mlflow_io.py`(MLflow I/O。`log_surrogate_model` は `surrogate.save` + `meta.yaml`(surrogate_type/dataset/train_comp_id) を artifact log、`load_surrogate_model` は `(NeuroSurrogateBase, SurrogateMeta)` tuple 返却) / `analysis/`(marimo 層。`access.py`=base_ui/setting_ui/draw_ui read 規約集約 (leaf、`target_of`/`train_of`/`current_of`/`dt_of`/`eval_comp_of`等。ui→mode import と衝突せぬよう独立) / `ui.py`=widget builder + 表示合成 (`view_result` が single/sweep 縦積み合成、Panel 組立。旧 view.py 統合、分離実益薄いため) / `save/`=永続化サブpkg (`panel.py`=Panel 表示 accumulator+SaveEntry+保存機構 / `restore.py`=状態 snapshot/復元。`to_meta` UI値→meta.json (run 選択は run_id 列で round-trip 可)、`make_panel` 復元 widget、`load`) / `mode/{single,sweep}.py`=per-mode compute+draw (`calc_eval`/`calc_sweep` = 置換後シミュ。calc gate と run load は薄 wrapper 廃止し marimo.py セルに inline、run load は `mlflow_io.load_from_selector`)。全 UI builder は preset 引数で meta.json 復元値を初期値上書き。依存: ui→save/mode、access は leaf、循環無) / `marimo.py` / `conf/`(`config.yaml` に共通 `surrogate.fit.optimizer` + `init.n_components` 既定 (1) 集約 + `surrogate/*.yaml` プリセット (base=`hh_full`/`traub_full` は lib=full の素体、type/n_components/preprocessor は `hydra.sweeper.params` 既定の `--multirun` で直積に振る。lib 本質差の `_hh_informed`/`_hh_relaxation` のみ個別)。`n_components` は `init` 直下 (preprocessor spec でなく)、library_specs は type+latent序数 `latents` のみ (省略=全latent。V/u/定数は項が固定保有))
