# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Coding Standards [WRITTEN BY HUMAN - DO NOT OVERWRITE]
- HH型マルチコンパートメントニューロンの一部ノードをSINDyで抽出したサロゲート方程式に置換し、演算コスト削減と波形再現性を評価する研究コード
- 一時変数は同じ値を何度も使うような場合にのみ許可
  - NG: `x = obj.attr; f(x)`
  - OK: `f(obj.attr)`
- 大きな改装が終わったら、just test でエラーが出ないことを確認,tests/ 以下のテストは自由に追加して良い ただし、20s以下に抑えること
- Hooksで実行されるjust lint、just formatのエラーは都度対処すること
- 研究のまとめは、docs/poster、docs/slideディレクトリ以下にtypstとしてまとめる

[以下のセクションは永続メモリとして上書きしても構いません。
ただし、30行以内程度に納め、本当に必要な情報だけを圧縮して書くこと]

## Commands

```bash
uv sync                                                           # 初期セットアップ（依存導入）
uv run scripts/main.py                                            # 実行 (fit+MLflow log のみ。kernel は回さない)
uv run scripts/main.py surrogate=_hh_informed                     # Hydraプリセット切替 (素体 base/hh/traub/traub19、lib違いは _hh_informed/_hh_relaxation 等)
uv run scripts/main.py --multirun                                 # preset の hydra.sweeper.params 直積 sweep (例 hh: n_components{1,2} × preprocessor{pca,ae} = 4 run)
just test                  # pytest (tests/、Hydraプリセット読込→fit→置換シミュ→指標/描画) + main.py + marimo export
just format && just lint   # ruff fix+format / ruff+mypy (strict、scripts/ 除外)
just mlflow                # MLflow UI (port 5100、backend: mlflow.db)
just marimo                # marimo notebook (port 2700)
just marimo-mcp            # Claude Code MCP連携 (port 2701)
just clean-cache / clean-log
```

## Architecture

**Data flow**: Hydra conf → `SurrogateBundle.setup(cfg)` [`DatasetConfig`/`NeuronGraph` → `unified_simulator()` JAX Euler+`lax.scan` → `ansatz.train_source(meta).stacked_gate()` で preprocessor build → `ansatz.fit()` (PySINDy → `FeatureLibrary` から `compute_theta` 直接構築)] → MLflow log。`neurosurrogate/__init__.py` で `jax_enable_x64` 強制 ON (HH ゲート exp 数値安定化に必須)。

**neurosurrogate/core/**: `network.py`(`Compartment`/`CompartmentType`/`NeuronGraph`/`DatasetConfig`/`CurrentConfig`。`chain(...)` ノード自動命名。置換の type 差替は surrogate/replace.py に移管、core は純粋データ構造) / `simulator.py`(`unified_simulator` + `calc_universal_deriv` が type 名 dispatch で `vmap` 一括計算、`generic_euler_solver` が `lax.scan`) / `coords.py`(xarray 座標の **write** 側。features=MultiIndex(comp_id,variable,gate)/gate=False→電位/node_id→I_internal を組立。`set_latent_coords`(V+latent 組立) + `transform_gate`(preprocessor で gate→latent 射影、ansatz/metrics 共用)) / `access.py`(同スキーマの **read** 規約を集約する唯一の場所。生 `sel` は全域禁止しここ経由。`potential`/`gate_matrix`/`comp_matrix`/`potential_matrix`/`i_ext_values`/`i_internal_values`/`time` 等 numpy accessor(計算層) + `trace`/`i_ext`/`i_internal` 等 (t,y) accessor(描画層)) / `opcost.py`(`OpCost` 代数、`+`/`*` で演算コスト集計)

**neurosurrogate/{compartments,models,currents}**: `compartments/{hh,traub,common}.py` 動力学 + `COMPARTMENT_TEMPLATES` (`hh.py` に `HH_RATE_ENTRIES`/`HH_GATE_PAIRS`/`HH_GATE_FORWARD`) / `models/` に `MCMODELS` + `traub19.py`(19-comp、traub.c 代数的等価) / `currents.py` に電流波形 + `CURRENT_MAP`

**neurosurrogate/surrogate/**: `meta.py`(`SurrogateMeta`=**学習構造の単一源**: surrogate_type/preprocessor_type (実装 dispatch キー)/n_components/dataset/**comp_type**/train_comp_id + `simulate`/`label`/`train_comp`/`original_opcost`。実装側は自分がどう選ばれたか知らない。**置換対象は `comp_type` (CompartmentType) が第一級で持つ** = サロゲートは「コンパートメントの種類 → それを置換するモデル」の対応で、MC ネットワーク名やノード名には紐づかない。yaml の `train_comp_identifier` (ノード名) は `build` の入口で種類へ解決して捨てる (yaml 側は不変)。以降 replace の型判定も hybrid の physics dispatch も `comp_type` 直参照で、`train_comp_id` の意味は「学習軌道を取った代表ノード = params/初期値の参照先」だけに縮む。**surrogate 内 leaf** → ansatz/replace がここと preprocessor だけ見れば済み bundle を知らない) / `bundle.py`(**サロゲートの主体**。`SurrogateBundle`(meta/preprocessor/closure を保持し ansatz を差替えて駆動するオーケストレーター。**3 属性ともクラスアノテーションで宣言し setup/load が代入** (`__init__` 引数なし=meta も他と同格)。**`train_xr` は保存せず `meta.simulate()` の cached_property** = 学習データは決定的に再現できるので実体を持たない → load 経路でも触れ (marimo が run ロード毎に学習データ図を描く)、参照しなければ simulate は走らない。`setup(cfg)` classmethod が**唯一の学習入口**で cfg の 3 ブロックを宛先へ振り分けるだけ (組み替えない): `meta`→`SurrogateMeta.build(**)` / `preprocessor`→`preprocessor_cls.fit(gate, meta.n_components, spec)` / `ansatz`→`ansatz.fit(meta,train_xr,prep,spec)`。**両 spec とも「種別固有 hyperparams のみの dict」で対称** (全種共通の n_components は meta 経由)。bundle に `fit` メソッドは無い。実装解決は `ansatz`/`preprocessor_cls` の cached_property 2 本が**どちらも meta の dispatch キー経由**で対称 (学習済みインスタンスは属性 `preprocessor`)。**対応表 `SURR_CLS`{sindy,hybrid}/`PREPROCESSOR_CLS`{pca,ae} は bundle.py に置く** — 読むのは bundle だけなので実装側は type 名を持たない (自分がどう選ばれたか知らない、を字義通りに)。`save`/`load`=`surrogate.joblib` 単一ファイル pickle。`surr_comp_type`/`opcost`/`metrics` は ansatz へ委譲。`ansatz` は meta.surrogate_type から解決する cached_property=状態なし→保存不要。**setup と load が別経路** → load は 3 点を戻すだけで simulate が走らない (未設定参照は AttributeError))。外部が触る型は一貫して `SurrogateBundle` (main.py/mlflow_io/analysis/metrics/replace すべて) / `ansatz/base.py`(`Ansatz[C]`=方程式の定式化 (列構造・kernel・演算コスト) のみ担う抽象基底。C=同定する `Closure` の具体型 (`SURR_CLS` は `type[Ansatz[Any]]`)。**状態を持たないストラテジ。bundle でなく meta/train_xr/preprocessor/closure を明示引数で受ける** (オーケストレーターへ依存を張り返さない): `train_source(meta)->TrainSource`/`train_inputs(meta,train_xr,prep)->TrainInputs`/`fit(meta,train_xr,prep,spec)->C`/`surr_comp_type(meta,prep,closure)`/`opcost(meta,prep,closure)` の5本のみ。**学習は 2 段 (gate→preprocessor / encode 後→closure) なので入力の型も 2 つ**、どちらも保存せず meta から引き直す: `TrainSource`(comp_ids/n_gate + `gate(ds,comp_id)`/`stacked_gate(ds)`)=**選択規則** (encode 前のゲートが要る preprocessor 学習用。setup も fit も view も同じものを引ける) / `TrainInputs`(x_names/u_names/x/u。時間軸も出所 comp も持たない=規則側が唯一の源)=**同定器へ渡す直前の実体**で、fit がそのまま流し view が同じものを描く (図と学習入力が食い違わない)。列構造は sindy=[V,g…]+u / hybrid=[g…]+V。軌道は comp ごとに分けたまま渡す (縦連結=境界に偽微分。単一軌道でも list のままで pysindy の速度差は無し)) / `replace.py`(置換ドメイン一元化。**互換性判定もここに集約** (ansatz でなく置換の関心事): `_PARAMS_MATCH` が surrogate_type → params 両立基準 (sindy=surr が param_cls=None で学習モデルが params 込み再現→全一致必須 / hybrid=物理 dv も Ca も置換先自身の params で解く→型一致のみ)、判定 3 本は **meta 受け** (ansatz からも呼ぶため): `replaceable(meta,comp)` **種類一致 (`comp.type != meta.comp_type` の直接比較)**+params 両立、`replaceables(meta,ds)` fail first (型一致だが params 非両立が有る or 対象皆無で `ValueError`)、`replaced_names(meta,net)` 非raise 診断用。`apply_surrogate(bundle,ds)` のみ bundle 受け (学習成果物から組む `surr_comp_type` が要る) で対象ノードを `replace_nodes`+`dc_replace` で非破壊 type 差替) / `ansatz/sindy.py`(`SINDyAnsatz`、列構造 [V,latent…,u]) / `ansatz/hybrid.py`(`HybridAnsatz`、列構造 [latent…,V] (u無)、AE preprocessor 利用可、n_components 3 が既定。学習データ源は `train_source`=`replace.replaceable` 真のノード全部 × 先頭 n_learned ゲート (Ca サブ系は physics 分離) (**置換する comp の軌道で学習する**→訓練分布=評価分布。単体モデルなら 1 個で従来同一、`model_name: traub19` 等 multi-comp 指定で全 comp の (V,gate) 軌道が入る。学習ゲートは params-free→全 comp 同一多様体、増えるのは V 被覆のみで n_components 据置き可)。preprocessor へは `stacked_gate` で**縦連結** (時間微分を取らない)、`fit` は**軌道リストのまま** pysindy へ (縦連結すると境界に偽微分)。**潜在方程式の右辺は `_dlatent(closure)->(latent,V)->dlatent` 1 点に閉じ** kernel 骨格/physics/初期値は表現非依存 → NN + ODE 解経由学習 (UDE) 化は `fit`/`_dlatent`/`opcost` の 3 本差替で済む)。各 ansatz は `train_source` (列選択) と fit 内 `Roles` を n_components から inline 構築 (sindy=学習元 1 comp × 全ゲート列) / `closure/`(**学習成果物 = ansatz が空けた穴を埋める閉包項の表現群**。ansatz/ (方程式の形)・preprocessor/ (gate↔latent) に並ぶ 3 本目の軸で、`preprocessor/` と同型 (ABC + 実装が 1 パッケージ)。`base.py`=`Closure` ABC。契約は**型を知らない側 (bundle) が呼ぶもの**だけ = `metrics()` 1 本 — ansatz は `Ansatz[C]` で具体型を知るので ξ/feature 展開も `opcost()` も契約に載せない (共通して持っていても型非依存の呼び出し側が要求しない限り契約にしない)。**何を食わせて同定したかも持たない** — 学習入力の選択規則は meta だけで決まり `Ansatz.train_source` から引けるので、成果物に焼き付けない。**閉包項は設定から dispatch されない** (ansatz が自分の型として作って返す) → bundle.py の対応表にも載らない唯一の軸。`sindy/`=SINDy 同定層。`roles.py`=**役割記号 `V,G,U = sp.symbols("V g u")` の唯一の定義源** (catalog はここから import)+`Roles`: V=電位列/g=ゲート列群(=隠れ変数)/u=外電流列。**feature 展開の語彙なので SINDy 固有** (NN 表現は連結ベクトルを受けるだけで役割記号不要) → closure/sindy/ 内。ただし**束縛の値 (どの index が V か) は ansatz が fit 内で構築して渡す** (列順は sindy=[V,g…,u]/hybrid=[g…,V] と定式化ごとに違う。SINDyBundle と同じ「型はここ・引数は ansatz」の関係)。`bindings(args,latents)` 1本で **束縛+展開本数**を決める (記号の意味は Roles だけが知る): U 持ちで u 無し→0本、G 持ち→選択 latent ごと1本、G 無し→1本。文字列キーは使わず Symbol のまま照合。`__init__.py`=`SINDyBundle`(`Closure` 実装。xi/feature 展開/compute_theta/opcost/metrics。`from_sindy` の x/u/t は list を渡すと複数軌道) + `OPTIMIZER_CLS`={"stlsq"}、項ライブラリはロジック/カタログ分離。項=1つの sympy 式が真実源→func(lambdify)/name(subs)/cost(op_cost)/args(free_symbols を V,g,u 順)/arity 全派生。HH レート関数は数値安定 exp 保持のため未定義 Function シンボル+lambdify 注入。`entry.py`=ロジック(`op_cost` 木辿り→OpCost/`LibraryEntry`(expr,args,func。args=Symbol tuple が列束縛キー兼 展開規則の判定源)/`_group_by_args` 同一 args で 1 束/`SubLibrary.expand(spec,roles)` は spec={type,latents?} のみ受け (番号=latent序数、既定全)、type 検証+群化のみ担い**展開本数と列束縛は `roles.bindings(args,latents)` へ委譲** (entry.py に V/g/u の分岐なし)/`FeatureLibrary.build(specs,roles)` は catalog を遅延 import) / `catalog.py`=データ(`_entry`(args 自動導出)+`_R` レート式+項カタログ `LIB_ENTRIES` 1本。**全 type は互いに素** (同じ式を2 type が持たない。合成は yaml 側の責務: 例 forward+backward=全レート、forward+relaxation_decay=緩和形)。`_rate_types(prefix,pairs)` が (alpha,beta) ペア列から {gate_forward,gate_backward,gate_forward_product,gate_backward_product,relaxation_decay} を派生 → `hh_*` (3ゲート) / `traub_*` (V依存8ゲート、Q は XI 依存で対象外) + gate_poly_volt (latent 高次・V積。1次は basis) + basis。レート実装注入は `_RATE_NS`=vars(hh)|vars(traub)、コストは entry.py の `_RATE_COST_MAP`=HH|TRAUB。basis=V/u/latent 射影+定数1(V に相乗り) で他項と同じ規則に還元、latents:[] で V/u/1 のみ))) / `preprocessor/`(gate↔latent 可逆変換、ansatz/ と同型パッケージ。`base.py`=`Preprocessor` ABC (encode/decode/metrics/opcost、`_set_fit_artifacts` で再構成統計+初期潜在。dataclass でなく明示 `__init__`) / `pca.py`=`PCAPreprocessor` / `autoencoder.py`=`AEPreprocessor`+JAX-AE 学習 / `fit(gate,n_components,spec)`=潜在次元は全種共通の明示引数・spec は種別固有のみ / レジストリは bundle.py 側)。**surrogate 配下は `closure/sindy/` を除き `__init__.py` を置かない** (namespace package。再エクスポートも type 名レジストリも持たせず、import は実体ファイルを直指す)

**neurosurrogate/{metrics,view}**: `metrics/wave.py`(eFEL スパイク + RMSE/MAE、V 抽出は `core.access` 経由) / `view/{engine,specs,model,train}.py`(`draw_engine`/`TraceSpec`(t,y numpy 保持で Dataset 非依存)/`PanelSpec`/`error_fig` は engine、`plot_2d_attractor_comparison`+`draw_all(original,surrogate,comp_id,get_preprocessed)` (内部 jobs dict で全描画一括、失敗を error_fig に畳み識別子付き fig 列返却) は specs、`view_neuron_graph`/`closure_figs` (閉包項の中身図。共通の図が無い→型で振り分け、非対応表現は空列) は model、`current_preview_fig`/`sweep_fig` (marimo 非依存の純粋描画。analysis は UI 値引き出しのみ担い委譲) は utils。`train_figs(bundle)` (**学習データ図**: train_raw=注入電流+学習 comp 全部の V+学習元 comp のゲート / train_preprocessed=**同定器へ渡す直前の x/u 全列** (`ansatz.train_inputs` を fit と共有) / train_recon=preprocessor 再構成 RMSE / train_v_coverage=V ヒスト (comp 別) / train_manifold=潜在軌道 (n_components<2 は V-g1)) は train。1 図 1 主題で分け、`draw_engine(spec, figsize)` に段数依存の寸法を渡す。**surrogate 単体依存**で置換シミュ不要 → `analysis/mode/single.py:model_figs` に入り run ロードのみで描画、データは `closure.train_source` + `bundle.train_xr` (meta から再生成) から組む。**view が (id,fig) 列を提供し analysis は保存/表示に流すだけ**。Dataset 掘削は `core.access` 経由)

**scripts/**: `main.py`(Hydra エントリ) / `mlflow_io.py`(MLflow I/O。`log_surrogate_model`=`bundle.save` を artifact log、`load_surrogate_model`=`SurrogateBundle.load`) / `analysis/`(marimo 層。**モデルペア = `comp_type → target model`** (左=置換対象のコンパートメント種類、右=`marimo.py:TARGET_MODEL` が種類ごとに定義する適用先 MC モデル一覧)。run 一覧は `runs_df` に `meta`(SurrogateMeta)/`comp_type` 列を持ち (`mlflow_io.get_runs_df`)、`make_setting_ui` が**選択ペアに実際に置換できる run だけ**を提示 = `replaced_names(meta, MCMODELS[target])` 非空で絞る (互換基準は replace ドメインのみが持ち UI に複製しない。例: traub→traub19 は params 自由な hybrid の run のみ残り sindy は落ちる)。`access.py`=base_ui/setting_ui/draw_ui read 規約集約 (leaf、`target_of`/`comp_type_of`/`current_of`/`dt_of`/`eval_comp_of`等。ui→mode import と衝突せぬよう独立) / `ui.py`=widget builder + 表示合成 (`view_result` が single/sweep 縦積み合成、Panel 組立。旧 view.py 統合、分離実益薄いため) / `save/`=永続化サブpkg (`panel.py`=Panel 表示 accumulator+SaveEntry+保存機構 / `restore.py`=状態 snapshot/復元。`to_meta` UI値→meta.json (run 選択は run_id 列で round-trip 可)、`make_panel` 復元 widget、`load`) / `mode/{single,sweep}.py`=per-mode compute+draw (`calc_eval`/`calc_sweep` = 置換後シミュ。calc gate と run load は薄 wrapper 廃止し marimo.py セルに inline、run load は `mlflow_io.load_from_selector`)。全 UI builder は preset 引数で meta.json 復元値を初期値上書き。依存: ui→save/mode、access は leaf、循環無) / `marimo.py` / `conf/`(**`surrogate` 直下は `meta`/`preprocessor`/`ansatz` の 3 ブロックのみ**で `SurrogateBundle.setup` の宛先と 1 対 1 (`meta`={surrogate_type,preprocessor_type,n_components,datasets,train_comp_identifier} / `preprocessor`={種別固有 hyperparams のみ、既定 `{}`} / `ansatz`={定式化固有 hyperparams のみ。sindy/hybrid では optimizer,library_specs})。`config.yaml` に共通 `meta.datasets.dt`/`meta.train_comp_identifier`/`ansatz.optimizer` を集約、`surrogate/*.yaml` プリセットが学習構造+library_specs を持つ (`base`/`hh`/`traub`/`traub19` が素体、`hydra.sweeper.params` で `meta.{n_components,preprocessor_type}` を `--multirun` 直積に振る。lib 本質差の `_hh_informed`/`_hh_relaxation` 等のみ個別)。library_specs は type+latent序数 `latents` のみ (省略=全latent。V/u/定数は項が固定保有))
