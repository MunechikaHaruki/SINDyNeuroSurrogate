type: hybrid
init:
  n_components: 1
  datasets:
    model_name: hh
    current:
      type: train
      params: {}
fit:
  preprocessor:
    type: ae
  library_specs:
    - type: hh_gate_forward           # α(V) 3個
    - type: hh_gate_backward          # β(V) 3個
    - type: hh_gate_forward_product   # α(V)·latent_1
    - type: hh_gate_backward_product  # β(V)·latent_1
    - type: basis                     # latent1, V, 1 (全入力)
