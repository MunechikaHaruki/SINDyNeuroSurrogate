type: hybrid
init:
  n_components: 1
  datasets:
    model_name: hh
    current:
      type: train
      params: {}
fit:
  preprocessor:
    type: ae
  library_specs:
    - type: hh_gate_forward           # α(V) 3個
    - type: hh_gate_backward          # β(V) 3個
    - type: hh_gate_forward_product   # α(V)·g1
    - type: hh_gate_backward_product  # β(V)·g1
    - type: basis                     # g1, V, 1 (全入力)

# スパイク応答が確認できず