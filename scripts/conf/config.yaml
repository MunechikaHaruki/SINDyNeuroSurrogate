defaults:
  - experiment: default
  - _self_ # ショートカット定義は最後に設定

hydra:
  run:
    dir: hydra-outputs/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: hydra-multiruns/${now:%Y-%m-%d_%H-%M-%S}

matplotlib_style: paper

seed: 6
simulater_dt: 0.01

dataset_profiles:
  steady_currents: [ 0, 3, 5, 6.5, 6.3, 6.2, 7, 10, 15, 20]
  random_current_seeds: [9919,9920]
