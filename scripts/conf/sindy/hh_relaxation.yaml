init:
  datasets:
    model_name: hh
    current:
      type: train
      params: {}
fit:
  preprocessor:
    type: pca
  library_specs:
    - type: gate_poly_volt
    - type: basis
      roles: [V, u]
    # --- dg0/dt 用 (relaxation形式) ---
    - type: hh_relaxation_driver
    - type: hh_relaxation_decay
