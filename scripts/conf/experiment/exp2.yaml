# @package _global_

defaults:
  - /datasets: default
  - _self_

experiment_name: exp2-hh_gauss_rand_pulse

datasets:
  train:
    data_type: hh
    current:
      _target_: neurosurrogate.utils.current_generators.hh_gauss_rand_pulse
      iteration: 900000
      mu: ${m}
      sigma: ${s}
      baseline: ${b}

m: 6.2
s: 5
b: 0

hydra:
  sweeper:
    params:
      s: 2,3,5,7
      sc: False