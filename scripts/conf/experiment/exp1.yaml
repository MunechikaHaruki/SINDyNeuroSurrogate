# @package _global_

defaults:
  - /datasets: default
  - _self_

experiment_name: exp1-hh_rand_pulse

datasets:
  train:
    data_type: hh
    current:
      _target_: neurosurrogate.utils.current_generators.hh_rand_pulse
      iteration: 900000
      baseline: 0