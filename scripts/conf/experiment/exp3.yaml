# @package _global_

defaults:
  - /datasets: default
  - /neurons: default
  - /models: default
  - _self_

experiment_name: exp3-hh_discretized

datasets:
  train:
    data_type: hh
    current:
      _target_: utils.current_generators.hh_discretized
      iteration: 900000
      options: [-5,1.3, 6.3,20]
      weights: [0.3,1,1,1]
      sigma: 1

hydra:
  sweeper:
    params:
      neurons.hh3.params.G_12: 1,0.5,0.3,0.1,0.05,0.025